[package]
name = "anvil"
description = "Командный центр Rust-программ"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true

[dependencies]
anvil-ui = { path = "../anvil-ui", features = ["serde"] }
anvil-update = { path = "../anvil-update" }
eframe = { workspace = true }
reqwest = { version = "0.13.5", default-features = false, features = ["rustls", "json", "blocking"] }
rfd = "0.17.2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.9.5"

[target.'cfg(windows)'.dependencies]
# Свой токен GitHub — в хранилище учётных данных Windows. На Linux keyring тянет zbus, чьи
# возможности расходятся с тем zbus, что уже подключает eframe; там токен берётся только у git.
keyring = "4.2.0"
windows-sys = { version = "0.61.2", features = [
    "Win32_Foundation",
    "Win32_System_Diagnostics_ToolHelp",
    "Win32_System_Threading",
] }

[build-dependencies]
anvil-ui = { path = "../anvil-ui" }
image = { version = "0.25.10", default-features = false, features = ["png", "ico"] }
winresource = "0.1.31"
